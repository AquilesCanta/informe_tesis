\ifx\all\undefined
\include{base}
\begin{document}
\fi
 
\chapter{Evaluación}

Este capítulo expone las pruebas realizadas sobre el sistema desarrollado y evalúa su comportamiento frente a distintas situaciones. El objetivo es encontrar limitaciones y mejores que realiza el diseño sobre el formato común.

\section{Prueba con transport streams}

\begin{itemize}
	\item Hay que ver qué pasa con el transporte de referencia
	\item Que pasa con uno con un montón de servicios
	\item Qué pasa cuando se deja de emitir los flujos elementales a alguno
	\item 
\end{itemize}

\section{Lista de servicios extendida}

Acá hay que hablar de la cantidad de servicios que pueden señalizarse en un ts a diferencia de uno común.

Limitación por ancho de banda para pmt, limitación por tamaño de sdt. Limitación por tamaño de pat. Limitaciones extras.

\section{Flujos elementales múltiples}

Hasta ahora es imposible incorporar distintos flujos elementales para un mismo servicio. Pero tampoco tendría sentido. Lo ideal sería mandar cada flujo de transporte en un grupo multicast distinto y que se logre algo similar a object based broadcasting.

\section{EPG y }


\section{emph{Closed Caption}}

La posibilidad de enviar \emph{Closed Caption} para servicios diferidos depende exclusivamente del formato, los cuales se pueden agrupar en dos categorías:
\begin{itemize}
	\item \textbf{Autónomos}: Existen algunos formatos de \emph{Closed Caption} (como CEA 608 y CEA 708\cite{ceaclosedcaption}) que no requieren señalización en la \pmt. Esta categoría de \emph{Closed Caption} funciona sin problema para servicios transportados por IP, debido a que viajan embebidos en el flujo de video del servicio. 
	\item \textbf{Señalizados}: Categorizamos en este grupo a los canales de \emph{Closed Caption} que deben ser referidos por la \pmt\ y pueden viajan en un flujo con su propio PID. Si se desea incluir este tipo de \emph{Closed Captions}, es necesario agregar una entrada a la \pmt\ extendida haciendo referencia al flujo por el que viajan, y tal flujo debe estar incluido en el flujo de transporte ISDB-Tb. Este tipo de flujos, al incluir únicamente teletexto, no hacen un uso considerable del ancho de banda de modo que no implican una degradación notable del sistema integrado. [TODO: Podría agregarse algún grafiquito para ponerle onda.] 
\end{itemize}

\section{Averiguar qué onda con el scrambling y el acceso condicional} 

\section{Redes son ambientes inseguros}

Internet es un entorno inseguro y, en su estado actual, el sistema integrado no posee ningún tipo de protección de privacidad. Esto quiere decir que, a diferencia de la señal ISDB-T que es broadcast, si es posible asociar un grupo multicast a un cliente, es posible también definir qué servicio está consumiendo tal cliente. 

% Supongamos que se está transmitiendo el servicio PEPE en el grupo multicast tal. Si logro capturar el join o algo... etc.
Por ejemplo, si a través de un software de \emph{sniffing} un atacante logra hallar el pedido de unión al grupo mulitcast de un servicio, también podrá identificar 


\section{Es lo mismo SD o HD para el ts}
% Un transport stream que refiere a dos servicios diferidos.
% Dos transport streams que refieren a dos servicios diferentes. Uno cada uno.
% Un transport stream que refiere a muchos servicios. Digamos 10.
% Un transport stream que refiere a 2 servicios. Uno de los cuales no tiene emisión.
% Closed caption
% Cómo se comportaría el acceso condicional con el esquema extendido
% Problemas de seguridad? Si alguien se pone a emitir algo en algún momento
% EIT debería funcionar
% Todo el resto de las cosas deberían funcionar. Ver el tema del bitrate puede ser más complicado.
% Con este esquema es imposible usar varios audios.
% También es imposible mandar video por multicast y audio por ts.
% Todos los flujos elementales se deben enviar por el mismo medio.
% Está bueno que no le interesa al ts si el servicio es HD o SD. Inclusive podría cambiar, supongo yo.
% Que pasa si dos empiezan a emitir al mismo tiempo?


\ifx\all\undefined
\end{document}
\fi